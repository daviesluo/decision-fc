{
  "name": "@bg/content",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "World data and localised copy. Decoupled from the engine so balance and text can iterate independently.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "build": "tsc -b", "typecheck": "tsc -p tsconfig.check.json" },
  "dependencies": { "@bg/engine": "workspace:*" }
}
