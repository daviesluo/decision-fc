{
  "name": "@bg/engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic football career simulation engine. Pure TypeScript: no DOM, no platform APIs, no I/O.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -p tsconfig.check.json"
  }
}
